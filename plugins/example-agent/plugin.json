{
    "name": "example-agent",
    "displayName": "工具调用 Agent 示例",
    "version": "1.0.0",
    "author": "gateway-team",
    "description": "演示 ctx.llm.runTools：让模型自主决定调用工具（如查天气、算数）再综合作答。移植 AstrBot agent 型 / 联网搜索类插件的参考样板。",
    "main": "index.js",
    "priority": 100,
    "enabled": false,
    "dependencies": [],
    "permissions": ["llm"],
    "config": {
        "maxSteps": { "type": "number", "default": 5, "min": 1, "max": 10, "description": "工具调用最大轮数（防止死循环）" }
    }
}
