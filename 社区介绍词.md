# 🌉 SillyTavern 多平台网关 —— 把你的 AI 老婆送到每一个聊天软件

> 「我在 QQ 上和师尊谈恋爱，在 Telegram 上和她喝下午茶，在 Discord 上和她一起打副本——而且是同一个她。」

## 一、这是个啥？

简单来说：**SillyTavern 多平台网关**是一座桥。

一边是你的 SillyTavern（后面简称 ST）——那个装着你精心调教的角色卡、世界书、预设的 AI 角色扮演引擎。另一边是 QQ、Telegram、Discord 三大聊天平台。

网关把两边连起来：你在 QQ 里发一句话，网关收到，喂给 ST，ST 让 AI 生成回复，网关再把回复发回 QQ。对你来说，就像在和一个真实的 QQ 好友聊天——只不过这个好友是你用 8 万字角色卡捏出来的。

**和老的 QQ 网关相比，它解决了这些痛点：**

- 🔌 动不动就断连？→ 指数退避重连 + 心跳检测，稳得像老狗
- 📝 长回复被截断？→ 自动分段，按各平台字数限制切割
- 🔀 想同时连 QQ 和 TG？→ 三平台并行，互不干扰
- 🧩 想加功能但不想改源码？→ 热插拔插件系统，从 GitHub 一键安装

---

## 二、怎么装？

### 第一步：装网关本体

1. 把 `sillytavern-gateway` 整个文件夹丢进 ST 的 `extensions/` 目录
2. 重启 ST，设置栏会多出一个「多平台网关」的图标
3. 点开它，你会看到连接状态面板——现在三个平台都是「离线」，别慌

### 第二步：配平台

#### QQ（OneBot v11 协议）

网关兼容 NapCat、Lagrange.OneBot 等主流 QQ Bot 框架。

1. 按你选择的框架文档，启动一个 OneBot v11 的 WebSocket 服务
2. 在网关面板填入 WebSocket 地址（正向）或让框架反向连接网关（反向）
3. 点「验证连接」，看到 QQ 变绿就成功了

#### Telegram

1. 找 @BotFather 申请一个 Bot Token
2. 填进网关面板的 Telegram 配置区
3. 点验证——恭喜，你的 AI 现在住在 Telegram 里了

#### Discord

1. 去 Discord Developer Portal 创建一个 Application，拿 Bot Token
2. 开启 Message Content Intent（这步容易漏，不开收不到消息）
3. 填 Token，验证，搞定

### 第三步：开启转发

面板顶部有个「网关模式」开关——**默认是关的**，这时候网关只待命不转发。

打开它，再打开「自动回复」，然后去 QQ 给你的 Bot 发条消息试试。

你应该会看到：消息注入 ST → AI 开始生成 → 回复发回 QQ。整个流程全自动。

---

## 三、怎么用？

### 基础操作

**你什么都不用做。** 打开网关模式后，正常在 QQ/TG/Discord 里聊天就行，网关在后台默默工作：

```
你（QQ）：今天天气真好
    → 网关收到 → 注入 ST → AI 生成回复 → 网关发回
AI（QQ）：是啊，阳光从窗棂洒进来，暖洋洋的……你要不要出去走走？
```

### 插件命令

网关支持 `/` 开头的斜杠命令，**这些命令只会在网关处理，不会发给 AI**（别担心 AI 会对你的 `/help` 逼逼叨）：

| 命令 | 作用 |
|------|------|
| `/help` | 看看都有啥命令 |
| `/status` | 网关状态一览 |
| `/clear` | 清空当前会话历史 |

不同插件还会注册自己的命令，装了就有。

---

## 四、什么场景下用得到？

### 场景 1：手机上玩 ST 角色

ST 是网页端，手机操作不方便。但 QQ 你总有吧？把角色接到 QQ 上，通勤路上、被窝里、摸鱼时，打开 QQ 就能和你的 AI 角色聊天，体验和真人 QQ 好友几乎一样。

### 场景 2：群里多人 RP

拉几个朋友建个 QQ 群，把 Bot 拉进去。AI 叙述剧情，群友各玩各的角色，AI 根据所有人的发言推进故事。配合下面的「抢答插件」，还能做成轮流发言的回合制 RP。

### 场景 3：多平台同步推剧情

你同时在 QQ 群和 Discord 服务器运营同一个角色剧情，用「记忆链接」功能让两边共享记忆——在 QQ 发生的剧情，Discord 那边的角色也记得。

### 场景 4：给群友当 AI 助手

不一定是角色扮演。把一个知识库角色卡接上，群友有问题直接 @Bot 问，比专门开网页方便多了。

### 场景 5：无人值守自动回复

睡觉了？上班了？Bot 替你在线 24 小时。有人发消息，AI 自动回复，醒来翻聊天记录就行。

---

## 五、特色玩法

### 🎮 玩法 1：Galgame 式选项拆分

装上 **option-splitter 插件**，AI 回复里的选项会自动拆出来，先发正文，再一条条补发选项——就像真的在玩 Galgame 一样：

```
AI: 「……你中午要不要和我一起吃饭？」她垂下眼睛，声音很轻。

    选项一：接受她的邀请
    选项二：婉拒
    选项三：拒绝并嘲笑她
    选项四：时间来到午餐结束后
```

正文先到，然后四个选项一条条蹦出来，节奏感拉满。

### 🖼️ 玩法 2：消息转图片

装上 **message-to-image 插件**，AI 的回复不再是一坨纯文本，而是渲染成精美的卡片图片发送。内置小说卡片、Galgame 对话框、终端暗色、极简白底四套模板，还能导入你在 ST 里写好的正则美化规则——服务器端用 Puppeteer 渲染，效果和 ST 里一模一样。

### ⚔️ 玩法 3：群聊抢答 RP

装上 **group-rp-bidding 插件**，AI 叙述完一段剧情后输出结束标记，插件自动开启 30 秒抢答窗口，第一个发言的群友胜出，他的行动被提交给 AI 推进下一轮剧情。轮流发言、节奏紧凑，群聊 RP 终于不乱了。

---

## 六：衍生生态

网关的插件系统是开放的，以下插件都可以在面板「从 GitHub 安装插件」里一键装：

| 插件 | 干嘛用的 | 安装地址 |
|------|----------|----------|
| **regex-filter** | 正则过滤 AI 输出，提取正文、移除标签，支持导入 ST 正则规则 | 内置 |
| **option-splitter** | 选项拆分发送，Galgame 式逐条补发 | `JOJO666888888/sillytavern-gateway-option-splitter` |
| **message-to-image** | 消息渲染为图片，支持自定义模板和 ST 正则 | `JOJO666888888/sillytavern-gateway-message-to-image` |
| **rp-memory** | RP 长期记忆，自动提取摘要、跨平台共享 | `JOJO666888888/rp-memory` |
| **st-data-manager** | 远程管理 ST 角色卡/世界书 + 多群聊角色路由 | 内置 |
| **group-rp-bidding** | 群聊抢答 RP，回合制剧情推进 | 内置 |

### 插件能干嘛？

- **出站过滤器**：在 AI 回复发出去之前加工它（拆选项、转图片、过滤标签、提取记忆……）
- **入站监听器**：在用户消息进来时做事（抢答判定、权限检查……）
- **斜杠命令**：注册自己的 `/xxx` 命令
- **定时任务**：cron 表达式定时执行
- **REST API**：暴露 HTTP 接口供前端调用
- **配置 UI**：在 `plugin.json` 里声明配置项，网关自动生成配置面板，不用手写前端

想自己写插件？网关面板里有「编写参考」按钮，一键下载完整的开发指南。

---

## 最后

这个网关的核心理念很简单：**不改 ST 源码，不侵入你的工作流，用插件实现一切**。

你精心调的角色卡、世界书、预设——一个字都不用改。接上网关，你的角色就活了，活在 QQ 里、Telegram 里、Discord 里，活在每一个你打开的聊天窗口里。

试试看吧。有问题、有想法、想写插件，随时来聊。

**仓库地址**：`JOJO666888888/sillytavern-gateway`

---

*「我做了这么好看的 AI 角色，为什么只能我自己在网页上玩？」—— 有了网关，不用了。*
